# Workflow: Build Resistance Bypass Engine
- Phase 1: Initialize `pyproject.toml` dependencies and `src/schemas/models.py`.
- Phase 2: Build resilient ID/API clients with shared pooling, selective retries, JSON-only caching, exhaustive pagination, and evidence provenance.
- Phase 3: Build the physical graph, require the requested-target component, discover intermediary targets, and compute fixed-transform priority components.
- Phase 4: Fetch indication/trial and pharmacology evidence for discovered targets; filter stopped, withdrawn, phase-1-only, and primary-drug records.
- Phase 5: Build FastAPI responses and UI with research-use warnings, no fabricated fallbacks, and thread-offloaded NetworkX math.
- Phase 6: Run deterministic unit/integration coverage plus opt-in live contract smoke tests before deployment.
